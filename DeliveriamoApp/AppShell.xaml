<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DeliveriamoApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DeliveriamoApp.View"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Home" >
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:Home}"
            Route="HomePage" />
    </FlyoutItem>
    <FlyoutItem Title="Login" >
            <ShellContent
            Title="Login"
            ContentTemplate="{DataTemplate local:Login}"
            Route="LoginPage" />
    </FlyoutItem>
    <!--<ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:Login}"
        Route="LoginPage" />
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:Home}"
        Route="HomePage" />-->

</Shell>
